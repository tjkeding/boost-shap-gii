<document_report>
  <meta project="boost-shap-gii" mode="document" submodule="supplemental" timestamp="2026-08-12T16:44:57Z" />
  <files_updated>
    <file path="README.md" changes="Added an 'Aggregate SHAP (Group-Level GII)' section documenting the aggregate_shap config block, constraints (disjoint membership, no nominal features, no group-name collisions, single-member advisory), the four aggregate effect types (singleton, within-group, between-group, group-by-ungrouped), and the block-permutation shadow strategy (Au et al. 2022, S1). This was a documentation gap from the prior /document run in this session, which missed the aggregate_shap feature due to context compaction.">
      <type>readme</type>
    </file>
    <file path="INPUT_SPECIFICATION.md" changes="Added: (1) full aggregate_shap config parameter table in Section 2 with validation rules and error types; (2) 'Aggregate SHAP (Group-Level Decomposition)' subsection in Section 4 detailing the aggregation operator, per-group effect-name conventions, synthetic feature-value axis, and block-permutation RNG seed convention; (3) block-permutation note in the Stage 4 shadow model description; (4) is_aggregate column in the Section 6 shap_stats_global.csv reference; (5) two aggregate-SHAP edge cases in Section 8 (large-group V artifacts, aggregate/member statistical non-independence, block-permutation conservatism direction).">
      <type>input_spec</type>
    </file>
    <file path="AID_LOG.md" changes="Rewrote the Session 2026-08-12 entry title and scope to include the aggregate_shap feature (previously omitted entirely from the session summary). Remediated 12 deduplicated LLM-attribution findings (10 Tier 1, 2 Tier 2) surfaced by 5 independent security-gate agents across Sections 2, 3, 4, and 7: reworded task descriptors adjacent to model names that the agents flagged as authorship-adjacent, despite none matching the literal Tier 1/Tier 2 regex patterns in CONVENTIONS.md (verified by direct regex re-check both before and after remediation). Per user decision, all flagged phrases were reworded rather than left as-is: 'authoring' -> 'structuring support' / 'updates'; 'drafting' -> 'updates'; 'documentation edits' -> 'documentation updates'; 'code generation' (bare) -> 'code scaffolding under direction'; 'implementation planning' -> 'implementation plan structuring support'; 'test design and test run orchestration' -> 'test suite execution support and test run orchestration'; Section 3 table header 'Function' -> 'Usage' (a second-pass Tier 2 flag on a header already changed once in the prior /document run this session).">
      <type>aid_log</type>
    </file>
  </files_updated>
  <aid_log>
    <status>updated</status>
    <sections_modified>Section 2 (Scope): one wording change. Section 3 (Tools Used): table header and one task-cell reworded. Section 4 (Development Workflow): one wording change. Section 7 (Development Session Log): current session entry rewritten for completeness; three prior session entries' LLM-tool-use bullets reworded for attribution-adjacent phrasing (content/meaning unchanged, task descriptors only).</sections_modified>
  </aid_log>
  <coverage>
    <public_functions_documented>n/a (this pass documents a config-driven feature and its algorithm, not individual function docstrings; source docstrings for _aggregate_effects, _is_aggregate_effect, _validate_aggregate_shap, and _block_permute_shadow were already written correctly during the original implement build)</public_functions_documented>
    <classes_documented>n/a</classes_documented>
    <modules_with_docstrings>n/a</modules_with_docstrings>
  </coverage>
  <summary>
    This supplemental /document pass closes a documentation gap identified by the user: the prior /document run in this session (report boost-shap-gii_document_20260812_152852.md) reported "no user-facing changes" and updated only AID_LOG.md and .aid/ artifacts, missing the aggregate_shap feature entirely due to context compaction during the session. A diff review against HEAD confirmed the actual change set was substantially larger than what the compacted summary preserved: a new aggregate_shap config block, group-level GII computation (_aggregate_effects in shap_utils.py), block-permutation shadow generation (_block_permute_shadow in utils.py, implementing Au et al. 2022 S1), pre-flight config validation (_validate_aggregate_shap in train.py), an is_aggregate output flag, a train_dir/run_dir context-key separation in infer.py, and an allowlist-to-blocklist refactor of CatBoost refit parameter handling in indiv_reports.py. README.md and INPUT_SPECIFICATION.md have been updated to document all of these. The mandatory Security Gate (5 independent agents) scanned README.md, INPUT_SPECIFICATION.md, and AID_LOG.md: zero PII/PHI findings; zero violations in README.md or INPUT_SPECIFICATION.md; 12 deduplicated LLM-attribution findings in AID_LOG.md (10 Tier 1, 2 Tier 2), all in pre-existing content from prior sessions. Orchestrator-side regex verification against the literal CONVENTIONS.md patterns found zero matches for any of the 12 findings, indicating the agents applied semantically broader judgment than the codified regex (consistent with their bias-toward-false-positives design). Findings were surfaced to the user rather than auto-dismissed or auto-remediated, per Tier 1/Tier 2 doctrine; the user elected to reword all flagged phrases despite the literal-pattern non-match, and remediation was applied and re-verified clean by a follow-up regex pass.
  </summary>
</document_report>
